# ── Safety Portal (Node/Cloudflare) build + local artifacts ──
node_modules/
dist/
.wrangler/

# Local Worker secrets (NEVER commit). Example template is tracked.
.dev.vars
.dev.vars.*
!.dev.vars.example

# Generated Worker binding types (regenerate via `npm run cf-typegen`)
worker-configuration.d.ts

# misc
*.local
.DS_Store
