import type { FieldopsApp, FieldopsGates } from "./fieldops_gates";

/** Field-ops Equipment READ routes. STUB (Brief 0) — implemented in Brief B. */
export function registerEquipmentRoutes(_app: FieldopsApp, _gates: FieldopsGates): void {
  /* implemented in Brief B */
}
